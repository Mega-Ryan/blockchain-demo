extends layout

block content
  .container
    h1 #{__('Blockchain Demo')}
  
    div.center-align
      img.picture(src='/images/wallpaper.jpg', alt='Background Picture', style='width: 100%; height: 50%;')
